# Loaded automatically by Vite when you run `npm run dev` (development mode only).
# Production builds use `.env.production` / CI env — not this file.
# Override any value in `.env.local` (gitignored).

VITE_API_URL=http://localhost:3000
VITE_SITE_DOMAIN=airestro360.local
